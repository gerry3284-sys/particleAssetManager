package com.particle.asset.manager.enums;

public enum AssetOperations
{
    OK, NOT_FOUND,
    BAD_REQUEST, ALREADY_EXISTS,
    STATUS_ERROR, CANNOT_UPDATE,
    INVALID_RAM, INVALID_STORAGE,
    INVALID_DATE, NO_ASSET_OR_TYPE_FOUND,
    INVALID_ASSET_OR_TYPE_VALUE,
}